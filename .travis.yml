matrix:
  include:
    - language: java
      script:
        - mvn clean verify

    - language: android
      jdk: oraclejdk8
      android:
      component:
          - tools
          - android-26
          - build-tools-26.0.2
        before_script:
          - cd frontend-passenger/PassengerRideShare/
        script:
          - ./gradlew build connectedCheck
