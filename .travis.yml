matrix:
  include:
    - language: java
      script:
        - mvn clean verify
    - language: android
      jdk: oraclejdk8
      android:
        components:
          - tools
          - android-25
          - build-tools-25.0.3
      before_script:
        - cd frontend-passenger/PassengerRideShare
      script:
        - ./gradlew build connectedCheck
