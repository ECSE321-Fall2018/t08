matrix:
  include:
    - language: java
      script:
        - mvn clean verify

    - language: android
      jdk: oraclejdk8
      android:
	components:
          - build-tools-26.0.2
          - android-26
          - tools
        before_script:
          - cd frontend-passenger/PassengerRideShare/
        script:
          - ./gradlew build connectedCheck
